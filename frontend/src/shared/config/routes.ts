export const routes = {
  home: "/",
  projects: "/projects",
  projectDetail: "/projects/:slug",
  login: "/login",
  register: "/register",
  dashboard: "/dashboard",
  author: "/author",
  admin: "/admin",
  cmsPage: "/pages/:slug",
} as const;
